# 上游同步与本地差异维护规范

## 1. 目的与范围

本文件约定 `cc-connect` fork 从上游同步稳定版本、识别功能重复、保留本地差异以及完成集成验收的统一方法。

它适用于整个仓库的代码、配置、持久化数据格式、文档、测试和发布流程。每次同步的具体版本、差异清单和结论，应按照本文末尾的记录模板单独记录。

## 2. 基本原则

1. **稳定版本作为集成基线。** 以带正式版本号的上游稳定 tag 为新基线，不把 `main`、beta、RC 或仍处于预发布阶段的 release 分支当作稳定基线。
2. **按功能和行为比较。** 是否重复以用户可见行为、配置语义、数据兼容性和外部接口为判断依据，不以文件路径、函数名称或提交标题为依据。
3. **上游同功能优先。** 当上游实现已经覆盖本地功能，且兼容性、安全性和行为不低于本地实现时，删除或停止维护本地重复实现，使用上游实现。
4. **本地扩展建立在上游基础上。** 上游只提供底层能力、本地还提供额外行为时，保留上游基础实现，再把本地扩展适配到新的接口和数据模型上。
5. **保护未提交工作。** 不在有未提交改动的工作树中直接进行同步、冲突解决或重构；不得通过 reset、restore、stash、clean 等方式处理无关或并发改动。
6. **安全和数据兼容优先。** 不能因为上游有“看起来相同”的功能，就丢弃本地的权限收紧、监听保护、迁移逻辑或旧数据兼容处理，必须先证明行为等价。
7. **每次同步都可回溯。** 记录上游版本和 commit、本地基线、保留/删除/移植的功能、未决问题和验证证据。

## 3. 上游版本选择

### 3.1 稳定版本定义

可作为正式同步基线的版本必须同时满足：

- 使用正式稳定 tag，例如 `v1.5.0`；
- tag 不带 `-beta`、`-rc` 等预发布后缀；
- tag、release 页面和对应 commit 能相互核对；
- 记录同步时看到的 tag commit SHA，避免远端移动或缓存造成判断歧义。

以下版本只能用于提前研究和差异预览，不能直接作为稳定同步基线：

- `upstream/main`；
- 尚未完成正式发布的 `release/vX.Y.Z` 分支；
- `vX.Y.Z-beta.N`、`vX.Y.Z-rc.N` 等预发布 tag。

### 3.2 远端读取与核对

默认使用名为 `upstream` 的只读远端获取上游信息。正式同步前需要核对：

1. 本地记录的上游 tag 是否存在；
2. 本地 tag、远端 tag 和 release 页面指向的 commit 是否一致；
3. 当前 fork 的本地基线和工作树状态；
4. 目标稳定版本相对本地基线包含哪些提交。

## 4. 同步前置条件

### 4.1 隔离集成环境

同步前先检查并记录：

- 当前分支和上游分支；
- 工作树未提交改动；
- 当前本地 commit；
- 当前使用的上游基线。

如果工作树不干净，应保持原目录不动，在独立的干净集成环境中从本地 commit 或上游稳定 tag 开始工作。不要把临时 WIP、其他任务改动和上游同步混在同一组冲突中。

### 4.2 先建立差异清单

同步前分别列出：

- 本地基线之后的已提交功能；
- 当前工作树中的未提交功能；
- 上游稳定版本新增或修改的功能；
- 配置字段、持久化结构、接口和文档变化。

差异清单应以“功能”为一行，而不是以“文件”为一行。

## 5. 功能对比与决策规则

每项差异至少记录以下字段：

| 字段 | 内容 |
| --- | --- |
| 功能 | 用户能感知的能力或行为 |
| 本地实现 | 提交、文件、配置和数据影响 |
| 上游实现 | 上游提交、文件、配置和数据影响 |
| 关系 | 重复、上游基础+本地扩展、行为冲突、本地独有、上游独有或已过时 |
| 决策 | 采用上游、移植本地、人工重构、保留双方或删除 |
| 兼容性 | 配置、旧数据、接口和用户行为影响 |
| 验证 | 定向测试、CUJ、构建、人工验收和剩余风险 |

### 5.1 完全重复

如果上游已经实现相同的用户行为，并且满足本地的配置、兼容性和安全要求：

- 采用上游实现；
- 删除本地重复代码；
- 只保留仍然需要的本地文档、测试或兼容适配；
- 在同步记录中说明本地实现被上游替代。

### 5.2 上游基础能力加本地扩展

如果两边处理的是同一个领域，但本地多出明确需求，例如上游提供话题隔离、本地增加激活状态持久化：

- 先采用上游的数据模型和基础流程；
- 再把本地扩展重新接入上游流程；
- 重点验证旧数据迁移、默认行为和异常路径；
- 不要直接用旧本地文件覆盖上游文件。

### 5.3 行为冲突

如果两边都实现了相近功能，但默认值、权限边界、错误语义或数据格式不同：

- 先列出用户可见差异；
- 以安全性、数据兼容性和明确需求为优先级重新决策；
- 必要时保留配置开关或迁移逻辑；
- 不能以“合并无冲突”代替行为验收。

### 5.4 本地独有功能

本地独有功能只有在仍有实际需求、维护成本可接受且不会破坏上游升级时才保留。保留后应尽量：

- 限制改动边界；
- 使用上游公开接口和能力接口；
- 补齐单元测试和必要的 CUJ；
- 判断是否适合整理为上游 PR，减少长期 fork 差异。

### 5.5 已过时功能

如果本地功能已不再需要、被新的上游机制替代，或维护成本大于收益，应明确记录后删除，不为了保留历史实现而继续携带。

## 6. 推荐集成方式

推荐采用“上游稳定版作为新基线，再逐项重放本地差异”的方式：

1. 从上游正式稳定 tag 建立干净的集成分支；
2. 按功能清单逐项检查，而不是一次性覆盖整个仓库；
3. 对完全重复功能直接采用上游；
4. 对重叠功能先接入上游基础，再移植本地扩展；
5. 对本地独有功能逐项移植，并保留对应测试；
6. 每完成一个功能类别就做一次小范围审查和验证；
7. 集成完成后，再将结果合回 fork 的主线或发布分支。

不推荐直接把上游大量历史提交整体合入当前主线后，再在一个巨大冲突提交中同时判断功能重复。这样会混淆上游变更、本地需求和冲突解决，也不利于回滚与审查。

## 7. 兼容性与验证

每次同步至少检查以下边界：

- 配置字段的默认值、废弃字段和向后兼容；
- Session、workspace、话题、权限状态等持久化数据；
- 卡片回调、消息路由、外部 API 和平台适配器；
- Agent CLI、App Server 或 provider 配置；
- 错误语义、权限边界和敏感信息处理；
- 中英文文档、配置示例、i18n 和 CHANGELOG。

验证遵循仓库现有测试约定：

- 运行受影响包的确定性单元测试；
- 变更 `core/engine.go`、`core/session.go` 或命令处理时，运行相关 CUJ；
- 执行必要的本地构建和 vet 检查；
- 不把本地静态检查、stub 或 isolated smoke 宣称为真实平台 E2E；
- 真实 IM 平台、provider 账号、安装的 Agent CLI 或外部回调验证，必须另行获得明确授权。

## 8. 每次同步记录模板

```text
同步日期：YYYY-MM-DD
上游远端：upstream
上游稳定版本：vX.Y.Z
上游 commit：<sha>
本地同步基线：<sha>

采用上游实现：
-

重新移植的本地扩展：
-

保留的本地独有功能：
-

删除或废弃的本地功能：
-

行为冲突与决策：
-

兼容性处理：
-

验证结果：
-

剩余风险和后续事项：
-
```

## 9. 同步完成后的收口

- 保留上游稳定 tag 原样，不移动或复用上游 tag；
- 在 fork 自己的记录中注明上游基线和本地差异；
- 对适合回馈上游的本地功能单独整理，避免下次继续产生重复实现；
- 发布前重新核对最终 commit、配置示例、文档、构建产物和远端状态；
- 未完成的迁移、验证或人工验收不得标记为同步完成。
